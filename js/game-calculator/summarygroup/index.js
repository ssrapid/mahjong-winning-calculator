export class SummaryGroup {
  constructor(){
    this.groups = [];
  }

  /**
   * 
   */
  push(keyObj, pattern) {
    const a = this.groups.find(g => {Object.entries(keyObj).every(([keyObj, v]) => {

    })});
  }

}